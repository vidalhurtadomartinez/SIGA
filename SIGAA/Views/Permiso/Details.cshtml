@model SIGAA.Models.Permiso

@{
    ViewBag.Title = "Detalle";
}
<h3><b>Información de Permiso</b></h3>

<hr />
<div>
    @Html.Partial("_DetailsDelete", Model)
    @if (FrontUser.TienePermiso(RolesPermisos.SEGU_permiso_puedeEditar))
    {<a href=@Url.Action("Index","Usuario",new { id = Model.iPermiso_id }) class="btn btn-primary"><span class="glyphicon glyphicon-pencil"></span> Editar </a>}
    <a href=@Url.Action("Index","Usuario") class="btn btn-default"><span class="glyphicon glyphicon-list"></span> Ir a Lista </a>
</div>