@using MvcFlash.Core.Extensions
@model SIGAA.Models.Usuario
@{
    ViewBag.Title = "Editar";
}
<h3><b>Editar Usuario</b></h3>
<hr />
@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    <div class="form-horizontal">
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        @Html.Flash()
        @Html.HiddenFor(model => model.iUsuario_id)
        @Html.HiddenFor(model => model.agd_codigo)


        <div class="form-group">
            @*@Html.LabelFor(model => model.agd_codigo, htmlAttributes: new { @class = "control-label col-md-2" })*@

            @*<div class="col-md-5 hidden">
                @Html.EditorFor(model => model.agd_codigo, new { htmlAttributes = new { @class = "form-control" } })
                @Html.ValidationMessageFor(model => model.agd_codigo, "", new { @class = "text-danger" })
            </div>*@

            @*<div class="col-md-5">
                <div class="input-group">
                    @Html.Editor("datos.nombreComleto", new { htmlAttributes = new { @class = "form-control", @placeholder = "nombre completo", @autofocus = "true" } })
                    <span class="input-group-addon"><span class="glyphicon glyphicon-search"></span></span>
                </div>
            </div>*@
        </div>

        @Html.Partial("_CreateEdit", Model)
        @*<hr />*@
        <div class="col-md-offset-2">
            <button type="submit" class="btn btn-primary" id="btnEditar"><span class="glyphicon glyphicon-save"></span> Guardar</button>
            <a href="/Usuario/Index" class="btn btn-default"><span class="glyphicon glyphicon-list"></span> Ir a Lista </a>
        </div>
    </div>
}

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}